---
"@datanet/p5": minor
---

Add binary pub/sub, DMX, and Art-Net support: `publishBinary`, `subscribeBinary`, `unsubscribeBinary`, `publishDmx`, `publishArtNet`, `buildDmxFrame`, and `buildArtDmxPacket` — matching the binary API available in `@datanet/core`.
